server.port=8080
spring.application.name=splitpay-frontend
spring.thymeleaf.cache=false

# Gateway URL — override via env var GATEWAY_URL in production/Docker
gateway.url=${GATEWAY_URL:http://localhost:8085}
